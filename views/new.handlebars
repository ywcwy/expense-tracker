<main>
  <form action="/expense" method="post" class="text-center pt-5">
    <p class="d-inline required">必填欄位*</p>
    <h4 class="d-inline ml-3">請輸入你的支出</h4>
    <div class="mt-2 mt-5 title">名稱* :<input type="text" name="name" class="ml-3" required="required">
    </div>
    <div class="mt-2 mt-4 title">日期* :<input type="date" name="date" class="ml-3" required="required">
    </div>
    <div class="mt-2 mt-4 title">類別* :<select type="text" name="category" class="ml-3" value="類別" required="required">
        <option value="家居物業">家居物業</option>
        <option value="交通出行">交通出行</option>
        <option value="休閒娛樂">休閒娛樂</option>
        <option value="餐飲食品">餐飲食品</option>
        <option value="其他">其他</option>
      </select>
    </div>
    <div class="mt-2 mt-4 title">金額* :<input type="number" name="amount" class="ml-3" required="required"></div>
    <div class="d-flex justify-content-center mt-5 pb-5">
      <button class="btn btn-warning create" type="submit">新增支出</button>
    </div>
  </form>
</main>